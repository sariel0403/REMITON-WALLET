import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { alignItem: "center", marginBottom: 50 },
  transferimg: {
    width: 250,
    resizeMode: "stretch",
  },
});

export default styles;
